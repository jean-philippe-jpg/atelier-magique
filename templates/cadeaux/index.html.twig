{% extends 'base.html.twig' %}

{% block title %}Cadeaux index{% endblock %}

{% block body %}
    <h1>Cadeaux index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Category</th>
                <th>Age</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for cadeaux in cadeauxes %}
            <tr>
                <td>{{ cadeaux.id }}</td>
                <td>{{ cadeaux.name }}</td>
                <td>{{ cadeaux.category }}</td>
                <td>{{ cadeaux.age }}</td>
                <td>{{ cadeaux.description }}</td>
                <td>
                    <a href="{{ path('app_cadeaux_show', {'id': cadeaux.id}) }}">show</a>
                    <a href="{{ path('app_cadeaux_edit', {'id': cadeaux.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_cadeaux_new') }}">Create new</a>
{% endblock %}
