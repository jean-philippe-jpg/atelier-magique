{% extends 'base.html.twig' %}

{% block title %}Cadeaux{% endblock %}

{% block body %}
    <h1>Cadeaux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cadeaux.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ cadeaux.name }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ cadeaux.category }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ cadeaux.age }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ cadeaux.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cadeaux_index') }}">back to list</a>

    <a href="{{ path('app_cadeaux_edit', {'id': cadeaux.id}) }}">edit</a>

    {{ include('cadeaux/_delete_form.html.twig') }}
{% endblock %}
