{% extends 'base.html.twig' %}

{% block title %}Elfes index{% endblock %}

{% block body %}
    <h1>Elfes index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Username</th>
                <th>Password</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for elfe in elfes %}
            <tr>
                <td>{{ elfe.id }}</td>
                <td>{{ elfe.username }}</td>
                <td>{{ elfe.password }}</td>
                <td>
                    <a href="{{ path('app_elfes_show', {'id': elfe.id}) }}">show</a>
                    <a href="{{ path('app_elfes_edit', {'id': elfe.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_elfes_new') }}">Create new</a>
{% endblock %}
